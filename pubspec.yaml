name: flutter_arcore_geospatial_playground
description: 'A new Flutter project.'
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  # Kotlin GradleなどAndroid 周りのバージョンが古いためPre Release版を使う
  arcore_flutter_plugin: ^0.2.0-alpha
  flutter:
    sdk: flutter
  vector_math:

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic_mono:

flutter:
  uses-material-design: true
